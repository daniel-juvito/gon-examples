# hello

Minimal happy-path program.

```bash
gon check hello.gon    # ok
gon build hello.gon
./hello
# hello, gon
# running gon on :8080
```

Shows:

- `!*string` parameter
- `!*Config` return
- required `!*string` struct field
